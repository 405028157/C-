http://c.biancheng.net/cpp/html/34.html

这个网址中详细讲了scanf的用法，但今天在看《C和指针》时发现了另一个容易犯错的地方：

char chp[20];
scanf("%s",chp);

在输入时，字符串之间不能用空格。

附上《C和指针》里的一段话

“使用所有格式码（除了%c）时，输入值之前的空白（空格、制表符、换行符等）会被跳过，值后面的空白表示该值的结束。因此，用%s格式码输入
字符串时，中间不能包含空白”
