下面四种定义字符指针数组的方式：

函数中定义：
char *a1[10] = { NULL };
char *a2[10] = { "sdjl" };
char *a3[10];

函数外定义：
char *a4[10];

字符指针数组 a1 中的字符指针全部被初始化为NULL
字符指针数组 a2 中的字符指针除了a2[0]被初始化为字符串“sdjl”的地址，其余字符指针全部被初始化为NULL
字符指针数组 a3 中的字符指针都没有被初始化
字符指针数组 a4 中的字符指针全部被初始化为NULL
