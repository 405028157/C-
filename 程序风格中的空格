《C和指针》中提到，程序中相邻的标记之间必须出现一至多个空白字符（或注释）（注释在编译之后会变成空格），不然它们
可能会被解释为单个标记。

下面三条语句是等价的：
y=x+1;
y = x + 1;
y = x
+
1;

而下面这组语句，前3条是等价的，但第4条是非法的：
int
x;

int   x;

int /*comment*/ x;

intx;

那么，下面的语句是不是合法呢？

int a   = 1;
int*pi = &a;

答案是合法的，尽管 int *pi = &a; 更为常见。 它中间用*隔开，并没有出现intpi这样的错误。
